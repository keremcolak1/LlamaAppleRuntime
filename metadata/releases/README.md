# Release Metadata

This directory is for checked-in release notes or metadata snapshots after the packaging repo is extracted.

Do not commit large artifacts here. Published XCFramework zip files belong on GitHub Releases. Local generated artifacts belong in `artifacts/`, which is ignored by git.

